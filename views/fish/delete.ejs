<!DOCTYPE html>
<html>
<head>
    <title>Delete Fish</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/home">← Dashboard</a>
        </div>
    </nav>

    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card border-danger">
                    <div class="card-body text-center">
                        <% if (fish.photo) { %>
                            <img src="<%= fish.photo %>" class="rounded-circle mx-auto d-block mb-3" style="width: 100px; height: 100px; object-fit: cover;">
                        <% } %>
                        <h3 class="text-danger">Delete "<%= fish.name %>"?</h3>
                        <p class="text-muted">Type: <%= fish.type %> | Price: ₹<%= fish.price %></p>
                        
                        <form method="POST" class="d-inline">
                            <button type="submit" class="btn btn-danger me-3">Yes, Delete</button>
                        </form>
                        <a href="/home" class="btn btn-secondary">Cancel</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
