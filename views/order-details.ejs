<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>📦 Order Details - Fish Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/home">
                <i class="fas fa-fish"></i> Fish Admin
            </a>
            <div class="navbar-nav ms-auto">
                <a href="/home" class="nav-link">🏠 Home</a>
                <a href="/orders" class="nav-link">📦 Orders</a>
                <a href="/userlist" class="nav-link">👥 Users</a>
                <a href="/logout" class="nav-link">🚪 Logout</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="row">
            <div class="col-12">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h2>
                        <i class="fas fa-receipt"></i> Order #<%= order._id.toString().slice(-8).toUpperCase() %>
                    </h2>
                    <a href="/orders" class="btn btn-secondary">
                        <i class="fas fa-arrow-left"></i> Back to Orders
                    </a>
                </div>

                <!-- ORDER SUMMARY -->
                <div class="row mb-4">
                    <div class="col-md-6">
                        <div class="card">
                            <div class="card-header bg-primary text-white">
                                <i class="fas fa-user"></i> Customer Details
                            </div>
                            <div class="card-body">
                                <p><strong>Name:</strong> <%= order.userId?.name || 'Guest' %></p>
                                <p><strong>Email:</strong> <%= order.userId?.email || '-' %></p>
                                <p><strong>Phone:</strong> <%= order.userId?.phone || '-' %></p>
                                <p><strong>Address:</strong> <%= order.userId?.address || '-' %></p>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="card">
                            <div class="card-header bg-success text-white">
                                <i class="fas fa-info-circle"></i> Order Info
                            </div>
                            <div class="card-body">
                                <p><strong>Date:</strong> <%= new Date(order.createdAt).toLocaleString('en-IN') %></p>
                                <p><strong>Total:</strong> <span class="h5 text-success">₹<%= order.totalAmount.toLocaleString('en-IN') %></span></p>
                                <p><strong>Status:</strong>
                                    <span class="badge <%= order.status === 'pending' ? 'bg-warning' : 
                                                     order.status === 'shipped' ? 'bg-info' : 
                                                     order.status === 'delivered' ? 'bg-success' : 'bg-secondary' %>">
                                        <%= order.status.toUpperCase() %>
                                    </span>
                                </p>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- ORDER ITEMS -->
                <div class="card">
                    <div class="card-header bg-info text-white">
                        <i class="fas fa-fish"></i> Order Items (<%= order.items.length %>)
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table table-hover">
                                <thead>
                                    <tr>
                                        <th>Fish</th>
                                        <th>Qty</th>
                                        <th>Price</th>
                                        <th>Total</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% order.items.forEach(item => { %>
                                        <tr>
                                            <td>
                                                <strong><%= item.fishId?.name || 'Fish' %></strong>
                                                <% if (item.fishId?.photo) { %>
                                                    <img src="<%= item.fishId.photo %>" alt="Fish" style="width: 50px; height: 50px; object-fit: cover; border-radius: 5px;">
                                                <% } %>
                                            </td>
                                            <td><span class="badge bg-primary"><%= item.quantity %></span></td>
                                            <td>₹<%= item.price.toLocaleString('en-IN') %></td>
                                            <td><strong>₹<%= (item.price * item.quantity).toLocaleString('en-IN') %></strong></td>
                                        </tr>
                                    <% }) %>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <!-- STATUS UPDATE FORM -->
                <div class="card mt-4">
                    <div class="card-header bg-warning text-dark">
                        <i class="fas fa-edit"></i> Update Order Status
                    </div>
                    <div class="card-body">
                        <form id="statusForm" action="/orders/<%= order._id %>/status" method="POST">
                            <div class="row">
                                <div class="col-md-4">
                                    <label class="form-label">New Status</label>
                                    <select name="status" class="form-select" required>
                                        <option value="pending" <%= order.status === 'pending' ? 'selected' : '' %>>Pending</option>
                                        <option value="shipped" <%= order.status === 'shipped' ? 'selected' : '' %>>Shipped</option>
                                        <option value="delivered" <%= order.status === 'delivered' ? 'selected' : '' %>>Delivered</option>
                                        <option value="cancelled">Cancelled</option>
                                    </select>
                                </div>
                                <div class="col-md-8 d-flex align-items-end">
                                    <button type="submit" class="btn btn-success">
                                        <i class="fas fa-save"></i> Update Status
                                    </button>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // AJAX Status Update
        document.getElementById('statusForm').addEventListener('submit', async function(e) {
            e.preventDefault();
            const formData = new FormData(this);
            
            try {
                const response = await fetch(`/orders/<%= order._id %>/status`, {
                    method: 'POST',
                    body: formData
                });
                const result = await response.json();
                
                if (response.ok) {
                    alert('✅ Status updated successfully!');
                    location.reload();
                } else {
                    alert('❌ Error: ' + result.error);
                }
            } catch (error) {
                alert('❌ Network error');
            }
        });
    </script>
</body>
</html>
